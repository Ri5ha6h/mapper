# Data Mapper DnD

Drag-drop mapper for JSON/XML trees. Upload source + target, map fields, review mapping list.

## Flow

- upload `.json` or `.xml`
- expand/collapse tree nodes
- drag source node -> target node to map
- remove mapping from list

## Dev

```bash
pnpm install
pnpm dev
```

Open `http://localhost:3000`.
